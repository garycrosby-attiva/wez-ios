// WezSpecs — capability contracts: protocols and types only.
// No concrete logic. Implementations live in WezImplementations;
// test doubles live in WezTestSupport.

public enum WezSpecs {
    public static let moduleName = "WezSpecs"
}
